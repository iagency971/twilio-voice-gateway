# E_INTRINSIC_SNAPSHOT_V1 — status

**Scope:** XAUUSD M1 BUY US 08:00–17:00 New York  
**Scientific state:** `PRE_OUTCOME_PACKAGE_FROZEN_AWAITING_REAL_LEDGER_QA`  
**Outcomes:** CLOSED  
**Pine production:** UNCHANGED

The methodological audit concluded that the existing `E_BUY_US` is an entry-context rank, not an intrinsic zone-strength score. E1/E2/E3 is display/localization order, not quality.

This package therefore builds an outcome-free intrinsic ledger from the already frozen v0.4 sticky candidate source. The only V1 model-eligible fields are `zone_width_v`, `episode_age_c5`, `current_family`, and `origin_family`.

The GitHub Actions workflow `xau-e-intrinsic-snapshot-v1.yml` must produce `E_INTRINSIC_SNAPSHOT_V1_REAL_QA_PASS` before any DEV outcome may be opened. After that PASS, the next step is a **Pro pre-outcome gate**, not immediate calibration.
