# Addendum C — Continuous OOS state provenance for C1/C5 cadence sensitivity

**Frozen:** 2026-08-26, after the H1 provenance hard-stop `19,913 != 19,878` and before any C1 H1/H2 reaction result is generated or inspected.

**Parent:** `XAUUSD_Z4_C1_REFRESH_EBUY_PREREG_v1_0_2026-08-26.md`

**Other frozen addenda:**
- Addendum A — `CAUSAL_ACTIVE_INTERVAL_V1`;
- Addendum B — causal sequential episode-state propagation.

## 1. Outcome-blind provenance issue discovered

The first causal-reaction runner rebuilt C5 monolithically from July 2024 through July 2025 for H1 and attempted to use that reconstruction as the source of the frozen H1 location state. This produced `19,913` eligible snapshots instead of the frozen OOS H1 count `19,878` and hard-stopped before any C1 reaction outcome was opened.

This mismatch is not a reaction result. It is a state-provenance mismatch.

## 2. Exact frozen OOS location provenance

The frozen E-BUY OOS coverage/table was generated by the published workflow `xau-z4-ebuy-coverage-oos-replication-v1-0.yml` from exactly 24 consecutive BID months:

`2024-08` through `2026-07`.

That workflow:
- acquires exactly those 24 source months with frozen hashes;
- builds continuous outcome-blind C5 Z4 geometry across the 24-month span;
- builds the frozen E-BUY state once across the continuous span;
- then subsets the already-built state into `OOS_H1`, `OOS_H2`, and `OOS_ALL`.

Therefore H2 is not a fresh state restart at 2025-08-01. It inherits causal scientific-state history from H1.

The frozen location evidence remains:
- H1 eligible snapshots = `19,878`;
- H2 eligible snapshots = `20,382`;
- candidate artifact SHA-256 = `157a5f180cc548f51ac9a0fd38ce9e031da48a0dad4fd4170f74b5836d4af90b`;
- frozen coverage result and provenance manifest remain authoritative.

## 3. Source-faithful reaction-target geometry

Historical H1/H2 reaction workflows did not use the 24-month OOS geometry object directly for target mapping. They generated target-month C5 geometry with causal month-local construction and then assembled the target months:

- H1 target months: 2024-08 through 2025-07; 2024-08 starts without a prior-month input and subsequent months use the immediately previous month as causal geometry warm-up;
- H2 target months: 2025-08 through 2026-07; every target month uses the immediately previous month as causal geometry warm-up.

This assembled monthly geometry is the source-faithful C5 reaction-target geometry and must be used for the causal C5 reaction control when available/reconstructed from the frozen workflows.

## 4. Corrected continuous cadence experiment

To avoid confounding cadence with a window restart, the cadence experiment will use one continuous raw chronology from `2024-08-01T00:00:00Z` through `<2026-08-01T00:00:00Z` for scientific state construction.

### C1
- Build C1 continuously over the same 24-month chronology.
- Apply the already-frozen equalized C1 warm-up of 480 C1 landmarks at the start of the continuous study only.
- Do not restart C1 state or warm-up at the H1/H2 boundary.
- H1 and H2 are reporting/evaluation subsets of this single causal state stream.

### C5 causal control
- Displayed E-BUY zones come from the exact frozen OOS candidate table/state, with frozen provenance guards.
- Empty eligible C5 states and the exact eligible snapshot chronology are reconstructed from the exact source-faithful C5 geometry/state pipeline and must reproduce the frozen H1/H2 coverage anchors before the causal C5 control is evaluated.
- Reaction target geometry uses the source-faithful monthly H1/H2 geometry described above.
- Only the symmetric `CAUSAL_ACTIVE_INTERVAL_V1` timing and Addendum-B sequential episode-state propagation are applied to the causal C5 reaction control.

## 5. Frozen historical baseline remains separate

`FROZEN_C5_BASELINE` remains an immutable provenance anchor and may be **consumed** rather than recomputed, exactly as permitted by Section 8 of the parent preregistration.

Its required anchors remain unchanged:
- H1: 19,878 eligible snapshots / 16,895 contacts / 7,127 BULL_REJECTION / TP1 resolved `0.3143902095934731`;
- H2: 20,382 eligible snapshots / 17,578 contacts / 7,643 BULL_REJECTION / TP1 resolved `0.3012963205447165`.

The consumption gate must verify the frozen coverage result/provenance, frozen candidate artifact hash, and exact published reaction anchors. It must not claim to have reproduced the historical baseline if it only consumed it.

## 6. Common-anchor detector control

The previously frozen mechanical C1 and mechanical C5 detectors remain the common-anchor engineering parity control. No detector parameter is changed by this addendum.

The C1-vs-C5 detector geometry invariant at common eligible 5-minute timestamps remains mandatory.

## 7. Interpretation discipline

No C1 reaction result had been opened when this addendum was frozen.

The valid cadence-effect comparison remains `CAUSAL_ACTIVE_INTERVAL_V1_C1` vs `CAUSAL_ACTIVE_INTERVAL_V1_C5`, after the frozen C5 provenance gate and common-anchor geometry gate pass.

No Pine change or production promotion is authorized by this addendum.